# C++コンパイラ
CXX = icpc
#CXX = g++


# 「-I」「-D」以外のコンパイラオプションを指定
OPTIONS =
OPTIONS += -O3 -Wall -xHost -openmp
#OPTIONS += -O3 -Wall -fopenmp
#OPTIONS += -fvisibility=hidden


# ライブラリのインストール場所をフルパスで指定
POLYLIB_DIR    = $(HOME)/Polylib-2.2
TEXTPARSER_DIR = $(HOME)/opt_intel/TextParser

# Octreeライブラリは, MRpFTT_SGI_20080428で動作確認
#OCTREE_DIR     = $(HOME)/sgi-octree


DEFINES =
#DEFINES += -DCUTLIB_TIMING
#DEFINES += -DCUTLIB_OCTREE
#DEFINES += -DCUTLIB_DEBUG

